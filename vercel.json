{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install --legacy-peer-deps && prisma generate && prisma db push && prisma db seed",
  "framework": "nextjs",
  "env": {}
}
